<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="BluetoothAudioCodec.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Bluetooth Audio Codec"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt" />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" Background="Transparent" Loaded="OnRootLoaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBarGrid" Padding="18,0" Background="Transparent">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <FontIcon
                    FontSize="16"
                    Glyph="&#xE702;"
                    IsHitTestVisible="False" />
                <TextBlock
                    x:Uid="AppTitle"
                    FontSize="13"
                    FontWeight="SemiBold"
                    IsHitTestVisible="False"
                    Text="Bluetooth Audio Codec" />
            </StackPanel>
        </Grid>

        <ScrollViewer
            Grid.Row="1"
            HorizontalContentAlignment="Stretch"
            HorizontalScrollMode="Disabled"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollMode="Auto"
            VerticalScrollBarVisibility="Auto">
            <Grid
                x:Name="ScrollContentHost"
                Padding="32,26,32,32"
                HorizontalAlignment="Stretch">
                <StackPanel
                    MaxWidth="780"
                    HorizontalAlignment="Stretch"
                    Spacing="16">

                <Grid Margin="0,0,0,4" ColumnSpacing="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="5">
                        <TextBlock
                            x:Uid="HeaderTitle"
                            Style="{ThemeResource TitleTextBlockStyle}"
                            Text="Active Bluetooth codec" />
                        <TextBlock
                            x:Uid="HeaderSubtitle"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Read the codec Windows negotiated with your headset."
                            TextWrapping="Wrap" />
                    </StackPanel>

                    <Border
                        x:Name="StatusBadge"
                        Grid.Column="1"
                        Padding="10,5"
                        VerticalAlignment="Top"
                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                        CornerRadius="14">
                        <StackPanel Orientation="Horizontal" Spacing="7">
                            <Ellipse
                                x:Name="StatusDot"
                                Width="7"
                                Height="7"
                                VerticalAlignment="Center"
                                Fill="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock
                                x:Name="StatusText"
                                FontSize="12"
                                FontWeight="SemiBold"
                                Text="Ready" />
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Style="{StaticResource CardBorderStyle}">
                    <Grid RowSpacing="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="1" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid ColumnSpacing="22">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="78" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border
                                Width="72"
                                Height="72"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Background="{ThemeResource AccentFillColorSecondaryBrush}"
                                CornerRadius="36">
                                <FontIcon
                                    FontSize="28"
                                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                    Glyph="&#xE702;" />
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="3">
                                <TextBlock
                                    x:Name="CodecText"
                                    FontSize="40"
                                    FontWeight="SemiBold"
                                    MaxLines="1"
                                    Text="—"
                                    TextTrimming="CharacterEllipsis" />
                                <TextBlock
                                    x:Name="ProtocolText"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Bluetooth Classic · A2DP" />
                            </StackPanel>
                        </Grid>

                        <Border
                            Grid.Row="1"
                            Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                        <Grid Grid.Row="2" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <SymbolIcon
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Symbol="Audio" />
                            <StackPanel Grid.Column="1" Spacing="2">
                                <TextBlock
                                    x:Uid="OutputLabel"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="DEFAULT OUTPUT" />
                                <TextBlock
                                    x:Name="DeviceText"
                                    FontWeight="SemiBold"
                                    Text="Checking…"
                                    TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <Grid ColumnSpacing="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        x:Uid="DetectHint"
                        VerticalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="A short quiet tone helps Windows create a fresh stream."
                        TextWrapping="Wrap" />

                    <Button
                        x:Name="DetectButton"
                        Grid.Column="1"
                        MinWidth="150"
                        Click="OnDetectClicked"
                        Style="{StaticResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="9">
                            <ProgressRing
                                x:Name="DetectionProgress"
                                Width="16"
                                Height="16"
                                IsActive="False"
                                Visibility="Collapsed" />
                            <FontIcon
                                x:Name="DetectIcon"
                                FontSize="14"
                                Glyph="&#xE721;" />
                            <TextBlock x:Name="DetectButtonText" Text="Detect codec" />
                        </StackPanel>
                    </Button>
                </Grid>

                <InfoBar
                    x:Name="MessageBar"
                    x:Uid="MessageBar"
                    IsClosable="False"
                    IsOpen="False"
                    Severity="Informational"
                    Title="Information" />

                <Expander x:Uid="DetailsHeader" Header="Technical details">
                    <Grid Padding="0,12,0,4" ColumnSpacing="24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="1.4*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Spacing="4">
                            <TextBlock
                                x:Uid="StandardIdLabel"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="STANDARD ID" />
                            <TextBlock x:Name="StandardIdText" FontFamily="Cascadia Mono" Text="—" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock
                                x:Uid="VendorIdLabel"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="VENDOR ID" />
                            <TextBlock x:Name="VendorIdText" FontFamily="Cascadia Mono" Text="—" />
                        </StackPanel>
                        <StackPanel Grid.Column="2" Spacing="4">
                            <TextBlock
                                x:Uid="CodecIdLabel"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="CODEC ID" />
                            <TextBlock x:Name="VendorCodecIdText" FontFamily="Cascadia Mono" Text="—" />
                        </StackPanel>
                        <StackPanel Grid.Column="3" Spacing="4">
                            <TextBlock
                                x:Uid="ObservedLabel"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="OBSERVED" />
                            <TextBlock x:Name="ObservedAtText" Text="—" />
                        </StackPanel>
                    </Grid>
                </Expander>

                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <FontIcon
                        FontSize="14"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Glyph="&#xE946;" />
                    <TextBlock
                        x:Uid="FooterNote"
                        Grid.Column="1"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="The A2DP event is emitted when a stream opens or closes. HFP calls and LE Audio use different codecs."
                        TextWrapping="Wrap" />
                </Grid>

                <Border
                    Padding="14,12"
                    Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                    CornerRadius="8">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center" Spacing="2">
                            <TextBlock
                                x:Uid="SupportTitle"
                                FontWeight="SemiBold"
                                Text="Support development" />
                            <TextBlock
                                x:Uid="SupportDescription"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Optional support does not unlock app features or content."
                                TextWrapping="Wrap" />
                        </StackPanel>

                        <StackPanel
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Orientation="Horizontal"
                            Spacing="4">
                            <HyperlinkButton
                                Content="Ko-fi ↗"
                                NavigateUri="https://ko-fi.com/benli06" />
                            <HyperlinkButton
                                Content="爱发电 ↗"
                                NavigateUri="https://afdian.com/a/benli06" />
                        </StackPanel>
                    </Grid>
                </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
